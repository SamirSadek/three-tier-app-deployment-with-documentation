apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: bmi-health
type: Opaque
stringData:
  POSTGRES_USER: bmi_user
  POSTGRES_PASSWORD: 
  POSTGRES_DB: bmidb

# NOTE: Replace values before applying. Do NOT commit real secrets to git.
